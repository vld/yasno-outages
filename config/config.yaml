yasno:
  city_id: 25
  dso_id: 902
  group_id: "5.2"
  local_tz: "Europe/Kyiv"
  url: "https://app.yasno.ua/api/blackout-service/public/shutdowns/regions/{city_id}/dsos/{dso_id}/planned-outages"

outages_storage:
  type: file_storage
  path: !ENV ${FILE_PATH:./outages}

monitoring_storage:
  type: file_storage
  path: !ENV ${MONITORING_FILE_PATH:./monitoring}

notifier:
  type: telegram
  bot_token: !ENV ${TELEGRAM_BOT_TOKEN}
  chat_id: !ENV ${TELEGRAM_CHAT_ID}
  thread_id: !ENV ${TELEGRAM_THREAD_ID}

deye:
  app_id: !ENV ${DEYE_APP_ID}
  email: !ENV ${DEYE_EMAIL}
  password: !ENV ${DEYE_HASHED_PASSWORD}
  app_secret: !ENV ${DEYE_APP_SECRET}